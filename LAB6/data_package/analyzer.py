def calculate_mean(num_list):
    sum = sum(num_list)
    piece = len(num_list)
    return sum / piece

def find_maximum(num_list):
    return max(num_list)

def find_minimum(num_list):
    return min(num_list)